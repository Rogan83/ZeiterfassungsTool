<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ZeiterfassungsTool.MVVM.Views.Admin.AdminPageChoice"
             Title="AdminPageChoice">

    <Shell.BackButtonBehavior>
        <BackButtonBehavior IsEnabled="True" IsVisible="True" Command="{Binding BackButton}"/>
    </Shell.BackButtonBehavior>


    <Grid RowDefinitions="auto,auto,*">
        <Button
            Grid.Row="0"
            
            Text="zur Listanansicht"
            Command="{Binding GoToListView}"
            />
        <Button
            Grid.Row="1"
            Text="zur Kalenderansicht"
            Command="{Binding GoToScheduler}"
            />
    </Grid>

</ContentPage>