<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ZeiterfassungsTool.MVVM.Views.Admin.PageChoice"
             Title="PageChoice">

    <Shell.BackButtonBehavior>
        <BackButtonBehavior IsEnabled="True" IsVisible="True" Command="{Binding BackButton}"/>
    </Shell.BackButtonBehavior>


    <Grid RowDefinitions="auto,auto,auto,*">
        <Button
            Grid.Row="0"
            Margin="10"
            Text="zur Benutzer Listenansicht"
            Command="{Binding GoToListView}"
            />
        <Button
            Grid.Row="2"
            Margin="10"
            Text="zur Überstundenansicht"
            Command="{Binding GoToOvertimeView}"
            />
        <Button
            Grid.Row="1"
            Margin="10"
            Text="zur Kalenderansicht"
            Command="{Binding GoToScheduler}"
            />
    </Grid>

</ContentPage>